<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.aloha.teamproject.mapper.UserMapper">

	<resultMap id="userMap" type="Users">
		<id property="id" column="id" />
		<result property="username" column="username" />
		<result property="password" column="password" />
		<result property="name" column="name" />
		<result property="nickname" column="nickname" />
		<result property="status" column="status" />
		<result property="createdAt" column="created_at" />
		<result property="updatedAt" column="updated_at" />
		<collection property="authList"
					ofType="UserAuth"
					select="selectAuth"
					column="username">
		</collection>
	</resultMap>

	<resultMap id="authMap" type="UserAuth">
		<id property="id" column="id" />
		<result property="userId" column="user_id" />
		<result property="auth" column="auth" />
		<result property="createdAt" column="created_at" />
		<result property="updatedAt" column="updated_at" />
	</resultMap>

	<insert id="join">
        INSERT INTO user ( id, username, password, name, nickname )
        VALUES ( #{id}, #{username}, #{password}, #{name}, #{nickname} )
    </insert>

	<insert id="insertAuth">
        INSERT INTO user_auth ( id, username, auth )
        VALUES ( #{id}, #{username}, #{auth} )
    </insert>


	<select id="select" resultMap="userMap" parameterType="String"> 
		SELECT *
		FROM users
		ORDER BY created_at DESC
	</select>

	<select id="selectAuth" resultMap="authMap" parameterType="String">
		SELECT *
		FROM user_auth
		WHERE user_id = #{user_id}
	</select>

</mapper>
